#Использовать fs

&Желудь
Процедура ПриСозданииОбъекта(&Пластилин РегистраторКаталогаФайлов)
КонецПроцедуры

Функция ПрочитатьИзФайла(ПутьКФайлу, ПрочитатьВСоответствие = Ложь) Экспорт

	Если ФС.ФайлСуществует(ПутьКФайлу) Тогда
		ЧтениеJSON = Новый ЧтениеJSON();
		ЧтениеJSON.ОткрытьФайл(ПутьКФайлу);
		Содержание = ПрочитатьJSON(ЧтениеJSON, ПрочитатьВСоответствие);
		ЧтениеJSON.Закрыть();
		Возврат Содержание;
	КонецЕсли;
	
КонецФункции